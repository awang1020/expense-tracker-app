# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: expense-tracker
metadata:
  template: expense-tracker@1.0.0
services:
  web:
    project: .
    language: ts
    host: staticwebapp
    dist: dist
